# Build artifacts. The repo-root .gitignore only ignores /target at the root,
# so this crate's own target dir needs naming here.
target/
node_modules/

# Generated by `napi build` — regenerated by `npm run build`.
index.js
index.d.ts
*.node
