name: Audit
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  # Rerun periodically to pick up new advisories
  schedule:
    - cron: '0 0 * * *'
  # Run manually
  workflow_dispatch:

jobs:
  audit:
    name: CVEs
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    steps:
      - uses: actions/checkout@v4
      - name: Debug cargo-audit output
        run: |
          cargo audit --json > audit_result.json
          cat audit_result.json
      - uses: actions-rust-lang/audit@v1
        name: Audit Rust Dependencies
